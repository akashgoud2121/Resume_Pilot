
'use client';

import type { ResumeData } from '@/lib/types';

interface TemplateProps {
  data: ResumeData;
}

// This component is intentionally left blank as it is no longer used.
// The download logic now directly captures the selected visual template.
export function PdfTemplate({ data }: TemplateProps) {
  return null;
}
